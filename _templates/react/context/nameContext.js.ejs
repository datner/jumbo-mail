---
to: src/contexts/<%= name %>Context/<%= name %>Context.js
---

import { useStore } from "utils/useStore";
import <%= name %>Factory from "./<%= name %>Factory";

const initialState = () => ({
    value: 0
});

const [<%= h.inflection.classify(name) %>Provider, use<%= h.inflection.classify(name) %>, with<%= h.inflection.classify(name) %>] = useStore(
  <%= name %>Factory,
  initialState
);

export const <%= name %>InitState = initialState();

export { use<%= h.inflection.classify(name) %>, <%= h.inflection.classify(name) %>Provider, with<%= h.inflection.classify(name) %> };

export default <%= h.inflection.classify(name) %>Provider;
